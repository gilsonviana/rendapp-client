import React, { Component } from 'react'

class Default extends Component {
    render() {
        return (
            <div className="default">
                <h1>Default widgets for the dashboard</h1>
            </div>
        )
    }
}

export default Default